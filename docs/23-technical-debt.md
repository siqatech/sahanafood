# Deuda técnica

Registro vivo. Toda deuda aceptada entra aquí con fecha límite de pago. Deuda permitida por fase: ver specs/phases.

Reglas: la deuda que toca **dinero, tenancy o auditoría NO es aceptable nunca**. Deuda de UX/reportes/optimización sí, con límite.

Lo que NO es deuda y por eso no está aquí: lo que una fase posterior tiene asignado por planificación (compras y proveedores en F6, pagos online en F5, conectores reales de marketplace en F7). Eso es alcance pendiente, no un atajo que haya que pagar. Aquí solo entra lo que se hizo de una forma sabiendo que la buena es otra.

| ID | Descripción | Fase en que se aceptó | Interés (qué empeora) | Fecha límite | Estado |
|---|---|---|---|---|---|
| DT-02 | **Terraform de entorno dev no entregado** (T3.16). El despliegue no está descrito como código. | F3 | Mientras no exista, no hay entorno de destino: la medición de carga que vale para el SLO (DT-05) y la demo grabada dependen de esto. Además cada día que pasa, el entorno que se acabe montando a mano es más difícil de reproducir. | Antes de abrir F5 (**vencida**) | **Bloqueada — necesita credenciales cloud (propietario). Ya no bloquea una tarea: bloquea el CIERRE de F5** — pentest (T5.36), pilotos, canario completo y Lighthouse dependen todos de esto. **Mitigada en parte**: `docs/34-puesta-en-marcha.md` levanta el sistema completo en UNA máquina con Docker, verificado de punta a punta, así que los pilotos ya no esperan a Terraform — esperan a un servidor. Lo que sigue faltando es el entorno gestionado: alta disponibilidad, copias automáticas y reparto de tráfico** |
| DT-05 | **Los números de la prueba de carga salen de un contenedor compartido** (Postgres, Redis, API, worker y k6 en cuatro núcleos). Son un suelo, no un SLO. | F4 | Sirven para detectar regresiones, pero no permiten prometer latencia a un cliente. Si se firma un SLO antes de medir sobre la infraestructura real, se firma a ciegas. | Gate de F5, tras DT-02 | Abierta |
| DT-08 | ~~La tienda web no tiene ninguna prueba de navegador.~~ **SALDADA** (ADR-0018). Playwright acotado a `apps/web`, un solo motor, contra el build real y **bloqueante en CI**. Seis pruebas, 3,4 s: las tres clases de fallo que la deuda documentaba —host descartado, `S/ NaN`, `'use server'` con una constante— se comprueban ahora **por su síntoma**, que es lo que el cliente ve, y no por su causa técnica, que la próxima vez será otra. Además cubre la promesa de T5.14 de que la tienda **funciona sin JavaScript**, que hasta hoy era una frase en un documento. Al escribirlas apareció un cuarto fallo de la misma familia: el checkout decía «Cambiar dirección» a quien no había escrito ninguna, porque el estado se deducía de «tiene líneas» en vez de leer el bloqueador `NO_ADDRESS` que el servidor ya mandaba. | F5 | Cerrada en el gate de F5 | **Saldada** |
| DT-12 | **El número de pedido del papel no es el del sistema.** El POS imprime la comanda y la precuenta antes de sincronizar, cuando el pedido todavía no tiene el correlativo que asigna el servidor, así que usa un contador del dispositivo y lo marca como provisional. | F5 | Cuadrar los dos exigiría reservar bloques de correlativos por dispositivo mientras hay red y que el servidor los respete al sincronizar — es una regla de negocio con implicaciones en facturación, no un ajuste de formato. Mientras tanto el papel dice «Nº provisional del POS» para que nadie lo confunda con el del comprobante. | Antes del primer piloto | Abierta |
| DT-13 | **Las alertas de operación no tienen destinatario.** Tres reglas piden avisar a una persona y hoy el aviso se queda en un evento que nadie escucha: `order.acceptance_overdue` (RN-ORD-04, pedido sin aceptar pasado el plazo), `billing.document_rejected` (RN-BIL-02, la OSE rechazó el comprobante) y `billing.deferral_alert` (RN-BIL-03, el envío diferido se acerca al límite). El hecho SÍ queda registrado —`acceptance_alerted_at`, el estado del documento— así que se ve entrando al panel; lo que falta es que el panel te busque a ti. | F5 | Un aviso que exige que alguien esté mirando no es un aviso. El pedido vencido acaba en auto-rechazo y el comprobante rechazado se descubre cuando el contador cuadra el mes. El interés crece con el número de locales: con uno, el encargado lo ve; con diez, no. | F6 (con el módulo de notificaciones) | Abierta |
| DT-16 | ~~**Las aprobaciones de dos personas las podía dar una sola.**~~ **SALDADA en el acto** (no llegó a aceptarse como deuda: toca dinero). Los tres controles de doble aprobación —reembolso sobre el umbral (RN-PAY-03), descuadre de caja (RN-POS-02) y descuento sobre el umbral (RN-T08)— comprobaban menos de lo que su propio comentario decía comprobar. El reembolso solo comparaba dos identificadores **que escribe quien pide**: bastaba con poner el id de una compañera —que `GET /users` devuelve— para aprobarse mil soles sin que ella se enterara. Caja y descuento sí pedían PIN, pero **ni exigían que fuera otra persona** —el cajero ponía el suyo y firmaba su propio faltante— **ni miraban si quien firma puede firmar**: el PIN del cocinero valía. | F5 | Un control que una sola persona satisface no es un control: es una casilla que hace que nadie mire. Y lo que se firma solo es justo el faltante de caja y la devolución grande. | Saldada en F5 | **Saldada** — `DeviceService.authorizeApproval` exige las tres cosas a la vez (distinta persona, PIN con bloqueo por intentos, y el permiso que autoriza la acción) y los tres sitios pasan por ella. Permiso nuevo `cash.approve_difference`, que el cajero **no** tiene. Ocho pruebas nuevas, una por cada forma de saltárselo. |
| DT-17 | ~~**La cola de corrección de comprobantes no se podía corregir.**~~ **SALDADA en el acto.** RN-BIL-02 exige «rechazado por OSE → cola de corrección; NUNCA se pierde la venta», y la cola existía desde F4 sin forma de vaciarla: `retry` reenvía el mismo RUC que la OSE acaba de rechazar y `POST /documents` se niega porque la venta ya tiene comprobante. La venta no se perdía —la letra de la regla se cumplía— pero **no se podía facturar nunca**. La pantalla de operaciones incluso decía «hay que corregir y reenviar». | F5 | Una venta sin declarar no prescribe: se acumula hasta que el contador cuadra el mes, y para entonces el plazo de RN-BIL-03 ya venció. | Saldada en F5 | **Saldada** — `POST /documents/:id/correct`, pantalla `/panel/comprobantes` y `DocumentView` devolviendo a nombre de quién va el comprobante (no se puede corregir lo que no se ve). Conserva el número: darle uno nuevo dejaría un hueco en la serie. |
| DT-18 | ~~**Devolver dinero exigía un `curl`, y la devolución atascada no aparecía en ninguna parte.**~~ **SALDADA en el acto.** `POST /payments/intents/:id/refund` existía desde T5.05 y no lo llamaba nada en la interfaz. Debajo, dos datos escritos con su comentario y sin ruta que los devolviera: `refund_reason` («va al panel y a la auditoría» — solo iba a la auditoría) y `refund_attempts`/`refund_last_error`, que la migración 0020 describe como «una alarma operativa que alguien tiene que atender a mano» y que no aparecía en ninguna pantalla: el barrido se rendía tras cinco intentos, el dinero seguía retenido y el cobro se veía igual que cualquier otro. | F5 | Un cliente esperando su dinero llama; que nadie sepa por qué no le llegó convierte una incidencia de la pasarela en una queja contra el local. | Saldada en F5 | **Saldada** — sección de cobros en el detalle del pedido con la doble firma (aprobador + PIN), `GET /payments/refunds/stuck` y las devoluciones agotadas arriba de la columna de problemas de operaciones. |
| DT-19 | ~~**El contrato de auditoría no lo comprobaba nadie, y la auditoría no se podía leer.**~~ **SALDADA en el acto** (auditoría = deuda inaceptable por CLAUDE.md). `AUDITED_ACTIONS` decía enumerar lo que docs/14 obliga a auditar y **diez de sus diecisiete nombres no los emitía nadie** —`price.changed` frente al real `catalog.price_set`, etc.—; la constante no la usaba ningún camino de ejecución, así que parecía un contrato y era una nota. Encima, la única ruta de lectura devolvía las filas crudas con el actor en UUID. | F5 | Nadie echa de menos una línea de auditoría que no sabe que debería existir. Se nota el día que hay que demostrar quién cambió un precio. | Saldada en F5 | **Saldada** — `AUDIT_REQUIREMENTS` (requisito → nombres reales) con prueba que falla ante un requisito huérfano, `AUDIT_REQUIREMENTS_PENDING` para los dos sin emisor y su motivo, `AuditQueryService` resolviendo el nombre del actor con `LEFT JOIN`, y pantalla `/panel/auditoria` con filtro por las acciones que existen. |
| DT-20 | ~~**El reparto no tenía ninguna pantalla.**~~ **SALDADA.** El módulo entero existe desde T5.15 —repartidores, zonas, ranking de RN-DLV-01, estados, saldos contra entrega, liquidación contra caja— y no había forma de dar de alta a un repartidor, crear un envío ni asignarlo. En un producto para dark kitchens con delivery, el pedido se cocinaba, se empacaba y ahí se quedaba. | F5 | La comida sale igual, pero sin seguimiento para el cliente, sin que el efectivo del repartidor cuadre con la caja y sin histórico de tiempos de entrega. | Saldada en F5 | **Saldada** — `/panel/reparto` con las cuatro columnas, alta de repartidores, asignación con el motivo de cada candidato y liquidación contra caja abierta. Queda **PA-08** abierta: cuándo nace el envío es decisión de dominio y la spec 09 no la fija. |
| DT-21 | ~~**El buscador de pedidos nunca buscó.**~~ **SALDADA.** `/panel/pedidos` pasaba `search: q` y el cliente de API lo descartaba —llega por propagación de un objeto, así que el tipado no lo veía—, de modo que la pantalla devolvía la lista entera con cualquier término. **La prueba de navegador que lo vigilaba pasaba**: comprobaba que hubiera una primera fila, y con el buscador roto siempre la hay. | F5 | Es la pantalla que se abre cuando suena el teléfono: se atiende a un cliente mirando el pedido de otro. | Saldada en F5 | **Saldada** — el cliente envía `search`, y la prueba comprueba que el resultado sea el que se buscó, no que haya alguno. |
| DT-22 | ~~**El sistema dejaba de vender solo, y nadie podía verlo ni deshacerlo.**~~ **SALDADA.** La saturación de cocina pausa canales automáticamente (RN-KIT-04) y `pausedChannels` llevaba desde F5 el comentario «para el panel y el KDS» sin que ninguna ruta la expusiera. En el local se vive como que las ventas se paran de golpe sin explicación; y el encargado que se queda sin pollo tampoco podía cerrar un canal sin acceso al servidor. | F5 | Cada minuto con un canal cerrado que ya podría estar abierto es venta que no entra, y nadie lo está mirando porque no aparece en ninguna pantalla. | Saldada en F5 | **Saldada** — `GET/POST /orders/channel-pauses` con permiso propio `orders.pause_channels` (supervisor), motivo obligatorio al cerrar, caducidad opcional, y la sección Canales arriba de la torre de control diciendo quién cerró cada uno. |
| DT-23 | ~~**Los umbrales de capacidad de cocina solo se tocaban por API.**~~ **SALDADA.** Deciden cuántas ventas se dejan de aceptar en hora punta (RN-KIT-04) y no tenían pantalla; el histórico de niveles se guardaba «para discutir el umbral con datos, no a ojo» y no lo devolvía ninguna vista. | F5 | El dueño ve su negocio dejar de aceptar pedidos sin poder ajustarlo ni entender por qué. | Saldada en F5 | **Saldada** — `/panel/cocina` con carga por estación, los dos umbrales validados uno contra otro, el orden de cierre con la sugerencia por comisión y el histórico de cambios de nivel. |
| DT-24 | ~~**Un guardado fallido borraba todo lo escrito en el formulario.**~~ **SALDADA.** Una acción de servidor que devuelve error vuelve a renderizar la página, y los campos no controlados regresan a su valor por defecto. Afectaba a todos los formularios del panel; se veía en el de umbrales, que tiene cinco campos. | F5 | Equivocarse en un campo obliga a reescribir los demás, y a la segunda vez la gente deja de corregir y guarda cualquier cosa. | Saldada en F5 | **Saldada** en el formulario de umbrales: la acción devuelve los valores tecleados junto al error y el formulario los prefiere. El patrón queda para los formularios de varios campos que se añadan. |
| DT-25 | ~~**Rentabilidad y cuadre no tenían pantalla.**~~ **SALDADA.** `GET /analytics/profitability` —«qué marca gana dinero por qué canal», la pregunta que justifica una dark kitchen— y `GET /analytics/reconciliation` —cuya divergencia la spec 16 llama bug crítico— existían desde T4.29 sin que nada los pintara. | F5 | Seguir o no en un marketplace se decide mirando la facturación, que es el número que más engaña; y una diferencia entre lo vendido y lo declarado no la ve nadie hasta que el contador cuadra el mes. | Saldada en F5 | **Saldada** — `/panel/reportes` con la tabla ordenada por margen (no por facturación), aviso destacado de margen negativo, porcentaje en aritmética entera sobre bps, y el cuadre del día con las dos discrepancias enlazadas a Comprobantes. |
| DT-26 | ~~**El agente de IA no tenía ninguna pantalla.**~~ **SALDADA.** Identidad, reglas, versiones con publicación y rollback, fuentes, sandbox y presupuesto: todo construido en T5.19–T5.32 y todo inalcanzable. El agente habla en nombre del negocio por escrito a clientes reales, así que lo que dijera era lo sembrado el día del alta. | F5 | Un tono equivocado, una promesa que no se cumple o un tema del que no debería hablar no se podían corregir — y el agente seguía hablando. | Saldada en F5 | **Saldada** — `/panel/agente` por marca, con guardar/publicar separados, sandbox que devuelve la traza (regla, fuentes, validador, coste), presupuesto explicando que las reglas siguen aunque el modelo se apague, y alta de fuentes con el aviso de no escribir precios. Las reglas se ven; crearlas sigue siendo por API. |
| DT-27 | ~~**Conectar un canal y saber dónde vive la tienda solo se podía por API.**~~ **SALDADA.** `POST /integrations/connections` no lo llamaba nada —dar de alta un marketplace exigía un `curl` con el secreto de firma dentro— y la torre de control enseñaba los conectores degradados sin forma de reactivarlos. Peor: **no existía `GET /storefront/domains`**, así que el dato más importante de la tienda —su dirección— no lo devolvía ninguna ruta, y quien registrara un dominio y cerrara la pestaña perdía el token de verificación. | F5 | Un negocio nuevo no puede poner su tienda en marcha ni recuperar un canal caído sin acceso al servidor. | Saldada en F5 | **Saldada** — `GET /storefront/domains` con `storefront.read` y pantalla `/panel/canales`: conectores con su cortacircuitos explicado, pausar/reactivar, y dominios con el token de verificación a la vista (no es un secreto: va en un TXT). |
| DT-07 | **El costo de inventario es un snapshot del `unit_cost` vigente, no promedio móvil.** Lo exige RN-INV-04 para no falsear el margen histórico, pero el promedio móvil real llega con compras. | F4 | El food cost es correcto para lo ya consumido, pero no refleja variaciones de precio de compra hasta que exista el módulo de compras. | F6 (compras) | Abierta |

## Deuda pagada

| ID | Descripción | Cómo se pagó | Fecha |
|---|---|---|---|
| DT-14 | **La bandeja de conversaciones no tenía pantalla** (`specs/ux/06`, F5). El módulo y el agente estaban completos y probados desde T5.19–T5.31; lo que faltaba era el sitio donde una persona atiende. La consecuencia: **una derivación bot→humano no llegaba a ningún sitio** — el agente escribía el resumen, marcaba `handoff_at`, y nadie lo veía. | Pantallas `/panel/conversaciones` y su hilo. Construirlas destapó lo de siempre: **`handoff_summary` y `handoff_at` se escribían desde T5.28 y ninguna ruta los devolvía**, así que el traspaso con contexto —lo que evita que el cliente lo cuente todo otra vez, que es el momento exacto en el que la gente abandona— existía en la base de datos y no ocurría en la práctica. Ahora `ConversationView` los expone y el hilo pinta **el resumen antes que los mensajes**, con los datos ya capturados en una lista para no repreguntarlos. **Con la ventana de 24 h cerrada no se deja escribir texto libre** (RN-CNV-03): dejar pasar el texto y que Meta lo descarte en silencio es el peor de los dos mundos; la nota interna sí queda habilitada. La nota va en burbuja amarilla y con su autor escrito, porque «¿esto lo dijo la IA o una persona?» no puede depender de interpretar de qué lado está la burbuja. | F5 |
| DT-15 | **El centro de operaciones no existía, y sin él no había forma de aceptar un pedido.** `specs/ux/05` lo sitúa en F4 (básico) y F5 (completo) y ningún backlog lo incluyó — el mismo agujero que DT-09, encontrado por el mismo método. El efecto no era estético: los canales con aceptación manual dependían de que alguien llamara al endpoint a mano, y a los diez minutos el barrido de RN-ORD-04 los rechazaba solo. **Todo pedido manual acababa rechazado, no por decisión de nadie sino por falta de un botón.** | Pantalla `/panel/operaciones` con las tres columnas de la spec: **por aceptar** (con la cuenta atrás real hasta el rechazo automático y las excepciones destacadas arriba), **en curso** por estado, y **problemas** — webhooks muertos con su reintento, comprobantes rechazados por la OSE y conectores degradados. El plazo se resuelve con **el mismo criterio de especificidad que el servidor** y eso tiene prueba unitaria propia: una copia que se desvía enseña «te quedan 4 minutos» sobre un pedido ya rechazado. La columna de problemas **se degrada sola** si falta permiso de integraciones o facturación, en vez de romper la pantalla que sirve para aceptar. Solo se pinta lo que tiene un dato real detrás: una tarjeta de «POS offline > 30 min» sin nada que la alimente enseñaría un verde que nadie ha comprobado. | F5 |
| DT-04 | **La bandeja de excepciones tenía API y no tenía pantalla.** RN-ORD-10 exige que un pedido cuyo catálogo no sabemos mapear **no se descarte**: se aparta. Eso funcionaba desde F4 en la base de datos, y la única forma de sacarlo de ahí era llamar al endpoint a mano. Para el cliente que espera su comida, «perdido» y «apartado donde nadie lo ve» son lo mismo. | Pantalla `/panel/excepciones` con su detalle. Al construirla apareció que el payload crudo del canal —guardado desde F4 en `mapping_failed.data` precisamente para poder resolver— **no lo devolvía ninguna ruta**: `getTimeline` trae todos los campos del evento menos `data`. Se añadió `GET /orders/:id/exception`, con permiso propio (`orders.review_exceptions`) porque el payload lleva nombre y teléfono del cliente, y **solo mientras el pedido está en revisión**. La pantalla ofrece la carta **resuelta para el canal del pedido** y no la carta entera —un plato sin precio ahí volvería a fallar al resolver, con el operador convencido de haberlo arreglado—, resuelve los **modificadores obligatorios** (mapear a un pollo con «Tamaño» devolvía un 422 que no se podía arreglar desde la pantalla), y ofrece **recordar el SKU** para que el siguiente pedido no vuelva a caer aquí. Rechazar exige motivo: sin salida, una bandeja solo crece, y una bandeja que solo crece se deja de mirar. | F5 |
| DT-11 | **El KDS no podía deshacer.** `specs/ux/02-kds.md` pide deshacer durante 8 s tras avanzar un ticket y la máquina de estados de cocina solo iba hacia delante. | Transición inversa `resume_preparing` (`ready → preparing`) en `@sahana/domain`, `undoTicket` en Kitchen y evento `kitchen.order_resumed`. Deshace **de verdad**: el ticket retrocede y, si el pedido había pasado a «listo» por ese toque, vuelve a preparación con él — deshacer el ticket dejando el pedido en `ready` habría dejado a la cocina trabajando en algo que el resto del sistema da por terminado. Dos barreras: ventana de 30 s en el servidor (más que los 8 s de la pantalla, para que un desfase de reloj no invalide un deshacer legítimo) y el pedido tiene que seguir en cocina —`accepted`, `preparing` o `ready`—, porque a partir de `packed` está en una bolsa y probablemente en manos de un repartidor. Queda auditado: sin traza, los tiempos de cocina se vuelven negociables. La prueba de auditoría destapó de paso que `accepted` había que aceptarlo, porque el evento que mueve el pedido a `preparing` puede no haberse consumido todavía cuando el cocinero deshace al instante. | F5 |
| DT-09 | **No existía ninguna interfaz de administración ni de operación: ni panel, ni POS, ni KDS.** Las tres estaban ESPECIFICADAS y asignadas a F4–F5 y ningún backlog las incluyó; los gates no lo detectaron porque comprueban pruebas y cobertura, no superficie de spec. | En dos tandas. **Panel** (`apps/web/panel`): portada «hoy», carta con precios por canal y negocio, con sesión en cookies `httpOnly` y ocho pruebas de navegador. **POS y KDS** (`apps/pos`, ADR-0019): PWA con Vite, IndexedDB, service worker propio y sesión por dispositivo + PIN — venta con la carta descargada, cobro sin tocar la red, cola offline y tablero de cocina en tema oscuro. Antes hubo que soldar la pieza que faltaba: `authenticateDevice` existía sin ningún llamador HTTP, así que una tablet emparejada no tenía forma de autenticarse. Las dos cosas que quedaron fuera en la primera tanda —el deshacer del KDS y el cierre de caja por denominación— se cerraron después, en esta misma fase. | F5 |
| DT-10 | **No había forma de crear los datos de negocio: ni marca, ni local, ni zona, ni horario, ni categoría, ni producto.** Solo las semillas demo, con SQL directo y siempre la misma pollería ficticia. La spec 03 pide «CRUD empresas/marcas/locales/cocinas/estaciones/almacenes/zonas/horarios» y la spec 04 «CRUD completo»; de ambas solo se construyó la mitad de LECTURA, y T3.12b, T4.01 y T4.03 se dieron por finalizadas sobre esa mitad. | En dos mitades. **Organización**: `OrganizationAdminService` + `OrganizationAdminController` (empresa, marca, local, cocina, unión marca↔cocina, estación, zona y horario) con permiso `org.write`. **Catálogo**: `CatalogAdminService` + `CatalogAdminController` (categorías, productos, precios por ámbito, grupos y opciones de modificadores, unión producto↔grupo, componentes de combo) con `catalog.write`, `If-Match` sobre `row_version` y coherencia de marca comprobada en el servicio —las claves foráneas del esquema son por `(tenant_id, id)` y no impiden cruzar marcas—. Todo **idempotente por clave natural** (RUC, slug, SKU, ámbito del precio), y encima `setup-business` aplica un negocio entero desde un archivo. Las pruebas no comprueban que las filas se inserten: comprueban que lo creado sea lo que `getResolvedCatalog` devuelve, lo que `findCoverage` responde y lo que el total de un pedido cobra. | F5 |
| DT-00 | El worker no arrancaba ningún proceso de fondo: el relay del outbox y el barrido de aceptación existían pero nadie los ejecutaba. | `apps/api/src/workers/main.ts` con `PeriodicJob`, verificado contra BullMQ y Postgres reales y con apagado limpio ante SIGTERM. | F3 |
| DT-01 | **`pnpm audit` corría en CI como informativo (`\|\| true`), no como bloqueante.** Aceptada en F3 con fecha límite en el gate de F5. | Se saldó antes de tiempo, en el gate de F4, porque la auditoría destapó **9 avisos altos**, entre ellos una **inyección SQL en `drizzle-orm`** — y eso toca tenancy, que CLAUDE.md declara deuda inaceptable. Subidos `drizzle-orm` 0.38→0.45.2 y los paquetes de OTel a la línea 2.x; `multer` y dos transitivos de OTel fijados con `pnpm.overrides`. Con cero altos, el paso pasó a bloqueante: un gate que nunca falla no es un gate. | F4 |
| DT-00c | **Un modificador obligatorio sin elegir mandaba el pedido de marketplace a la cola de muertos.** `ModifierError` y `PricingError` viven en `@sahana/domain` y no heredan de `DomainError`, la jerarquía de la API, así que se escapaban por la rama de los fallos transitorios: cinco reintentos contra un payload que no iba a mejorar y `failed` al final. Rompía RN-INT-02 y el criterio de T4.13. | El `catch` de `resolveToOrder` los reconoce y los aparta a `needs_review` al primer intento, con prueba que falla si se revierte. Lo destapó T4.30: 133 envíos en `failed` con el mismo texto. | F4 |
| DT-03 | **Capacidad dinámica de cocina sin implementar.** El KDS no limitaba cuántos pedidos aceptaba según la carga real. Aceptada en F4 con vencimiento en F5. | Migraciones `0025`/`0026` y `SaturationService` (T5.18). Dos umbrales: el primero **extiende la promesa y sigue vendiendo** —un cliente al que le dicen 55 min no se va; uno al que le prometen 35 y llega en 55, sí—; el segundo **pausa canales** por orden de menor margen. La decisión vive en `@sahana/domain`, es pura y monótona (más carga nunca relaja el nivel), y el efecto es **idempotente**: extender solo al empeorar de nivel, porque el barrido corre cada 30 s y acumular quince minutos por vuelta acabaría prometiendo la comida para el día siguiente. La reapertura automática **no levanta una pausa manual**. | F5 |
| DT-00b | **Nadie procesaba los webhooks de marketplace en producción.** `IngestionService.processPending` solo se llamaba desde las pruebas: un pedido entraba, recibía su 202 y no llegaba nunca a la cocina. | Barrido `ingestion-sweep` cableado al worker, más `workers/wiring.test.ts`, que falla si un `PeriodicJob` se declara sin arrancarse o sin pararse al apagar. Lo destapó la prueba de carga (T4.30). | F4 |
